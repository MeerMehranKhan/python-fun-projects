This repo contains small Python-based fun projects I built while learning the language. Nothing fancy, just fun little games that cover the basics like functions, loops, user input, and a bit of tkinter for the GUI one.

Built with plain Python, no external libraries needed except tkinter (which comes bundled with most Python installations).